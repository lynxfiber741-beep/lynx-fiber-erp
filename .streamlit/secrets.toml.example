# Copy to secrets.toml (same folder). That file is gitignored — password stays on your PC only.
# Streamlit Cloud: paste DB_URL in app Settings → Secrets (not in GitHub).

DB_URL = "postgresql://postgres.PROJECT_REF:[YOUR-PASSWORD]@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require"
