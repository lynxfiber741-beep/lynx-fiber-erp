# Supabase — Session pooler (port 5432). Password MUST match Supabase Dashboard → Database.
# If login fails: Dashboard → Project Settings → Database → Reset database password → update below.

[database]
host = "aws-1-ap-southeast-1.pooler.supabase.com"
port = 5432
dbname = "postgres"
user = "postgres.hvnqenuoyaefojzshvik"
password = "HdNjGQHhdKAuu64W"
sslmode = "require"
